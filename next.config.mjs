/** @type {import("next").NextConfig} */
const config = {
  output: "export",
  trailingSlash: true,
  distDir: "docs",
  experimental: {
    // Add experimental features if needed
  }
};

export default config;
